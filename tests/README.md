# Tests

This project keeps automated tests under `tests/unit/`.

Legacy manual scripts that previously lived in `tests/` were removed because they were not part of CI and contained environment-specific or sensitive placeholders.

Use:
- `make test-unit`
- `make test-local`
